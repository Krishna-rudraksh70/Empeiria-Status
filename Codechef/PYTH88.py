# Create a string array with the given values
days_of_week = ["Monday", "Tuesday", "Wednesday", "Thursday"]

# Output the last two elements on separate lines
print(days_of_week[-2])
print(days_of_week[-1])
